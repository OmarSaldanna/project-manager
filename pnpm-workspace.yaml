packages:
  - "packages/*"
# Allowlist explícito de build scripts (política de seguridad).
# Solo paquetes confiables y necesarios. esbuild: runtime de bundling de vitest.
allowBuilds:
  esbuild: true
