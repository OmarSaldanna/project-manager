# Forzar pnpm — política de seguridad de la organización
engine-strict=true
# Reduce superficie de scripts post-install de dependencias no confiables.
# Allowlist explícito de paquetes que sí pueden correr build scripts:
# (se administra con `pnpm approve-builds`)
